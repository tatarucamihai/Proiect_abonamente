<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Proiect_abonamente.StartPage"
             Title="StartPage">
    <Grid RowDefinitions="Auto,Auto,Auto,*" VerticalOptions="FillAndExpand" RowSpacing="10" Padding="20">
        <Label Grid.Row="1" Text="Oh, hey Zoro!" HorizontalTextAlignment="Center" FontSize="30" Padding="0,120,0,0" TextColor="White" />

        <!--Description-->
        <Label Grid.Row="2" Text="Now you are ready to live with no excuses !" FontSize="18" HorizontalTextAlignment="Center" TextColor="White"/>

        <Button Grid.Row="3" Text="Let's Start!" FontAttributes="Bold" BackgroundColor="White" FontSize="18" HeightRequest="50" VerticalOptions="End" TextColor="#578894" HorizontalOptions="FillAndExpand"/>
    </Grid>
</ContentPage>